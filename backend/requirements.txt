# Core framework
fastapi==0.115.6
uvicorn[standard]==0.32.1

# Config & validation
pydantic==2.10.3
pydantic-settings==2.7.0

# Database
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
pgvector==0.3.6

# Document parsing
pdfplumber==0.11.4
python-docx==1.1.2

# File upload support
python-multipart==0.0.19

# Auth — Clerk JWT verification (RS256 via JWKS)
python-jose[cryptography]==3.3.0

# HTTP client (JWKS fetching)
httpx==0.28.1

# AI — OpenAI embeddings + GPT-4.1-mini
openai==1.59.3

# Rate limiting — per-user throttle on AI-heavy endpoints
slowapi==0.1.9
