// Tailwind v4 — this file is no longer the primary config.
// Theme, content detection, and variants are now configured in globals.css via @theme and @custom-variant.
// Keep this file only if a plugin requires it.
export default {};
